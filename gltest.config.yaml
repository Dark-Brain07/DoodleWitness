networks:
  default: studionet
  studionet:
    accounts:
      - "${DOODLEWITNESS_TEST_STEWARD_KEY}"
      - "${DOODLEWITNESS_TEST_REQUESTER_KEY}"
  localnet: {}
paths:
  contracts: contracts
  artifacts: artifacts
environment: .env
